# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ingestion (Python)
ingestion/.venv
ingestion/__pycache__
*.pyc

# B-Roll bridge (Python) -- see broll_bridge/README.md for setup
broll_bridge/.venv

# Local runtime data (lives in the OS app-data dir, but ignore just in case)
*.db
*.db-journal

# Rust build output (huge, regenerated by `cargo`/`tauri dev`)
src-tauri/target

# Camera/software manual PDFs -- large (800MB+) and third-party copyrighted;
# never belong in git. ingestion/ingest.py reads them from disk directly.
books/*.pdf

# Tauri bundler output
src-tauri/gen
